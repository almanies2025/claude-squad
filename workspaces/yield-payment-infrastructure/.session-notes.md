### Read These Files First (in order)

1. `journal/0042-CONNECTION-trust-plane-patterns-transfer.md` — 5 transferable patterns from FloatYield to next B2B financial product
2. `journal/0043-FIX-rate-environment-integration.md` — scenario_multiplier applied to all 3 models (this session)
3. `journal/0041-FIX-naive-ci-double-scaling.md` — Naive CI double-scaling fix (σ√h·√h → σ√h)
4. `journal/0040-DECISION-capital-sequencing.md` — ILC/CUSO/Sponsor Bank sequencing; fatal flaw chain
5. `apps/web/app/page.tsx` — frontend React component (futuristic neon-cyber redesign)
6. `apps/web/app/layout.tsx` — uses next/font/google (Inter, JetBrains_Mono, Orbitron)
7. `apps/web/app/globals.css` — dark void theme CSS variables
8. `scripts/monitor.py` — 4 monitoring gates
9. `.session-notes.md` (this file) — current state

### Accomplished

- **Rate environment integration (Pattern 4 closed)**: `backend/app/main.py:463-510` — `scenario_multiplier` applied as parallel shift to all 3 model outputs. `yield_rate` in API response now reflects scenario-adjusted rate. Stress=0.85×, Upside=1.10× verified.
- **Futuristic frontend redesign**: `apps/web/app/page.tsx` + `globals.css` — dark void background (#060912), neon cyan/purple/green palette, Orbitron display font, JetBrains Mono for data, animated live-dot status, scenario pills (◆ BASE / ▼ STRESS / ▲ UPSIDE), neon glow bars with 80% CI overlay bands.
- **Layout.tsx fix**: Removed Google Fonts `@import` (fails in offline env). Uses `next/font/google` for Inter, JetBrains_Mono, Orbitron. CSS vars map `--font-orbitron`, `--font-inter`, `--font-jetbrains-mono`.
- **Journal 0043**: Documents the rate environment integration fix.

### Outstanding

- [ ] **Windows dual-server setup**: Frontend (`npm start` on :3000) and backend (`python -m uvicorn` on :8000) must run in **two separate PowerShell windows simultaneously**. Both must stay open. User had trouble with this — keep terminal windows open.
- [ ] Capital sequencing decision (journal 0040): CUSO-first path recommended; user must confirm

### Oversight — Verify Before Starting

- [ ] Backend loads: `cd backend && python3 -c "from app.main import app; print('OK')"`
- [ ] Backend running: `curl -s http://localhost:8000/health`
- [ ] `yield_events` has 195 rows (3 accounts × 65 events)
- [ ] WSL2 frontend: `cd apps/web && npm run build && npm start` (NOT `npm run dev`)
- [ ] Naive CI ratio: h=60 span / h=7 span ≈ √60/√7 = 2.93×

### Oversight — Verify After Completing

- [ ] All 3 accounts return differentiated model outputs
- [ ] Holt intervals widen with horizon: 7d→638, 14d→659, 30d→704, 60d→781
- [ ] Stress scenario: base→stress ratio = 0.850× (±1bps)
- [ ] Upside scenario: base→upside ratio = 1.100× (±1bps)
- [ ] `recon_gap_description` shows "Measured gap" for accounts with ≥5 yield_events rows
- [ ] Demo banner: "models are unvalidated against real Fed rate environments"

### Blockers

- Windows dual-server setup requires 2 terminals open simultaneously — confirmed working, user needs to keep both windows open
