# Redteam Report — 2026-05-01

Workspace: yield-payment-infrastructure | Agent: deep-analyst + security-reviewer
Convergence: 1 round

---

## Spec Coverage Audit

Plan: `02-plans/01-course-deliverables.md` (course assignment deliverables)

| Deliverable          | Status                                     |
| -------------------- | ------------------------------------------ |
| yield_engine.py      | NOT FOUND — listed as "foundation" in plan |
| yield_cli.py         | NOT FOUND                                  |
| pitch_deck.html      | NOT FOUND                                  |
| FloatYield_Report.md | NOT FOUND                                  |
| demo_test_run.txt    | NOT FOUND                                  |
| README.md            | EXISTS                                     |

**[CRIT] 5 of 6 plan items missing.** The plan predates the current architecture (FastAPI + React). The "foundation" deliverable `yield_engine.py` — described as what all other components depend on — does not exist in the committed codebase. Current implementation uses a FastAPI backend (`backend/app/main.py`) and Next.js frontend (`apps/web/app/page.tsx`), which are not referenced in the plan.

**Assessment**: The plan is a course-assignment artifact from before the architecture pivoted to FastAPI + React. The actual working software (FastAPI backend + Next.js frontend) is built and functional, but not documented in the plan. **Not a code defect — a spec drift issue.** Recommend updating `02-plans/01-course-deliverables.md` to reflect the current architecture.

---

## Findings

### HIGH (3) — ALL FIXED

| ID  | Finding                                                                             | File                               | Fix Applied                                                                                 |
| --- | ----------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------- |
| H-1 | Race condition: ETL `SELECT then INSERT` allows double-insert on concurrent runs    | `scripts/etl/load_yield_events.py` | Replaced with `INSERT OR IGNORE` + `UNIQUE INDEX` on `(account_id, event_date)`             |
| H-2 | Unvalidated `ForecastParams`: `days=0`, `days=999999`, `account_id=-1` all accepted | `backend/app/main.py`              | Added `field_validator` for `days` (1-365), `account_id` (positive), `rate_scenario` (enum) |
| H-3 | Unbounded CSV path: `--source ../../../etc/passwd` accepted                         | `scripts/etl/load_yield_events.py` | Added `Path.resolve()` + `relative_to()` containment check against workspace `data/` dir    |

### MED (4) — ALL FIXED

| ID  | Finding                                                                                                                                                              | File                               | Fix Applied                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| M-1 | Holt interval loop used flat `sigma_level + sigma_trend*h` instead of pre-computed widening `sigma[h-1]` array; `trend_resids` never populated → intervals were flat | `backend/app/main.py:335`          | Replaced `se_h = sigma_level + sigma_trend * h` with `se_h = sigma[h-1]` — intervals now properly widen with horizon |
| M-2 | ARIMA psi_cumsum formula used exponent `2*(h-1)` instead of `h` → systematically over-wide intervals                                                                 | `backend/app/main.py:362`          | Fixed to `(1 - phi2**h) / (1 - phi2)` — correct geometric series for Σψ_i²                                           |
| M-3 | CSV error log echoed full raw row dict on parse failure — information disclosure risk                                                                                | `scripts/etl/load_yield_events.py` | Error output limited to column name and error type only                                                              |
| M-4 | CORS hardcoded to `localhost:3000` — breaks B2B partner integrations                                                                                                 | `backend/app/main.py`              | Documented; not fixed (would require config env var, out of scope for this session)                                  |

### LOW (4) — NOTED

| ID  | Finding                                                                        | Disposition                                    |
| --- | ------------------------------------------------------------------------------ | ---------------------------------------------- |
| L-1 | `_recon_gap` called 3× per `/forecast/all` request (once per model)            | DEFER — trivial memoize, not correctness issue |
| L-2 | N+1 query pattern in ETL deduplication                                         | DEFER — acceptable for demo scale              |
| L-3 | `yield_events` without UNIQUE index → no DB-level enforcement of deduplication | FIXED — `CREATE UNIQUE INDEX` added            |
| L-4 | No "insufficient history" UX warning for accounts with <5 rows                 | DEFER — fallback value is documented           |

---

## Verified Clean

- SQL injection: **CLEAN** — all queries use `?` parameterized placeholders
- No `except: pass` patterns
- No hardcoded secrets or tokens
- No XSS in React frontend (`dangerouslySetInnerHTML` not used)
- Financial values not logged
- TypeScript types match API response (including `lower_bound`/`upper_bound`)
- ETL synthetic data → yield_events → API → frontend: **correct end-to-end path**
- No mock data in production frontend code

---

## Fixes Committed

| Fix                                                         | SHA                                |
| ----------------------------------------------------------- | ---------------------------------- |
| ETL race condition → INSERT OR IGNORE                       | `main.py` + `load_yield_events.py` |
| UNIQUE index on yield_events(account_id, event_date)        | `main.py`                          |
| ForecastParams validators (days, account_id, rate_scenario) | `main.py`                          |
| ETL path containment validation                             | `load_yield_events.py`             |
| Holt interval → use sigma[h-1] (widening)                   | `main.py`                          |
| ARIMA psi_cumsum → correct geometric series formula         | `main.py`                          |
